---
import config from '../content/config.json';
import works from '../content/works.json';
import '../styles/global.css';

// categoryTaglines: 默认文本(英文) + 对应的 i18n key
const categoryTaglines: Record<string, { text: string; i18nKey: string }> = {
  'Thematic Experience Design': { text: 'Immersive Visual Storytelling', i18nKey: 'index.tagline_thematic' },
  'Gameplay Interaction Design': { text: 'Precise Combat Interactions', i18nKey: 'index.tagline_gameplay' },
  'Monetization UX Design': { text: 'Elegant Monetization Design', i18nKey: 'index.tagline_monetization' },
  'Cross-Platform UX Design': { text: 'Seamless Multi-Device UX', i18nKey: 'index.tagline_crossplatform' },
  'System Design': { text: 'Scalable Design Systems', i18nKey: 'index.tagline_system' },
  'Creative Experience Solution': { text: 'Innovative Player Experiences', i18nKey: 'index.tagline_creative' },
};

// categoryDescriptions: 首页卡片"设计大类"层面的描述（与 works 页的具体项目描述区分开）
const categoryDescriptions: Record<string, { text: string; i18nKey: string }> = {
  'Thematic Experience Design': {
    text: 'Building immersive thematic visual systems through IP collaborations and seasonal packaging — translating narrative concepts into cohesive interface languages, motion identities and emotional player journeys.',
    i18nKey: 'index.category_desc_thematic',
  },
  'Gameplay Interaction Design': {
    text: 'Designing precise, fluid and distraction-free combat and core mechanic interactions across shooting, strategy and action genres — keeping players fully immersed in high-intensity moments.',
    i18nKey: 'index.category_desc_gameplay',
  },
  'Monetization UX Design': {
    text: 'Elegantly integrating monetization systems into the player journey — simplifying payment paths, refining visual presentation and shaping flows that create a win-win for users and business.',
    i18nKey: 'index.category_desc_monetization',
  },
  'Cross-Platform UX Design': {
    text: 'Tailoring optimal interaction solutions across mobile, PC, console and XR — adapting input characteristics and usage scenarios while keeping the experience consistent and recognizable.',
    i18nKey: 'index.category_desc_crossplatform',
  },
  'System Design': {
    text: 'Architecting comprehensive UI component libraries, design tokens, typography and spacing systems — ensuring visual consistency across products while accelerating team collaboration and delivery.',
    i18nKey: 'index.category_desc_system',
  },
  'Creative Experience Solution': {
    text: 'Delivering full-cycle creative solutions for novel gameplay and brand experiences — from user flow and visual storytelling to interactive prototyping, turning complex mechanics into intuitive, engaging journeys.',
    i18nKey: 'index.category_desc_creative',
  },
};
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{config.site.name}</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Inter:ital,opsz,wght@0,14..32,300;0,14..32,400;0,14..32,500;0,14..32,600;0,14..32,700;0,14..32,800&display=swap" rel="stylesheet">
</head>
<body>
  <!-- 顶部导航 -->
  <header class="site-header" id="siteHeader">
    <div class="header-inner">
      <a href="/" class="logo">
        <img src="/common/logo.webp" alt="XUYANG UX DESIGN" class="logo-img" />
      </a>
      <div class="nav-right">
        <nav class="main-nav" id="mainNav">
          <a href="/" class="nav-link active" data-i18n="nav.home">Home</a>
          <a href="/works" class="nav-link" data-i18n="nav.work">Work</a>
          <a href="/about" class="nav-link" data-i18n="nav.about">About</a>
        </nav>
        <button class="lang-toggle" id="langToggle" aria-label="Toggle language">
          <img src="/common/translate-icon.webp" alt="Language" class="lang-icon" />
          <span class="lang-label">EN</span>
        </button>
        <a href="/about#contact" class="nav-cta">
          <span data-i18n="nav.contact">Contact Me</span>
          <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 17L17 7M17 7H7M17 7V17"/></svg>
        </a>
        <button class="hamburger" id="hamburger" aria-label="Toggle menu">
          <span class="hamburger-line"></span>
          <span class="hamburger-line"></span>
          <span class="hamburger-line"></span>
        </button>
      </div>
    </div>
  </header>

  <script>
    const header = document.getElementById('siteHeader');
    if (header) {
      let lastScroll = 0;
      window.addEventListener('scroll', () => {
        const currentScroll = window.scrollY;
        if (currentScroll > 80) {
          header.classList.add('scrolled');
        } else {
          header.classList.remove('scrolled');
        }
        lastScroll = currentScroll;
      });
    }

    const hamburger = document.getElementById('hamburger');
    const mainNav = document.getElementById('mainNav');
    if (hamburger && mainNav) {
      hamburger.addEventListener('click', () => {
        hamburger.classList.toggle('active');
        mainNav.classList.toggle('open');
      });
      mainNav.querySelectorAll('.nav-link').forEach(link => {
        link.addEventListener('click', () => {
          hamburger.classList.remove('active');
          mainNav.classList.remove('open');
        });
      });
    }
  </script>

  <!-- 视频背景加载优化脚本 -->
  <script>
    (function() {
      const heroBg = document.querySelector('.hero-bg');
      const video = document.querySelector('.hero-video') as HTMLVideoElement;
      if (!heroBg || !video) return;

      // 1. 网络检测：慢速网络不加载视频，保持深色背景
      const conn = (navigator as any).connection;
      if (conn && (conn.saveData || conn.effectiveType === '2g' || conn.effectiveType === 'slow-2g')) {
        video.remove();
        return;
      }

      // 2. 动态设置视频源（避免浏览器在解析 HTML 时提前触发加载导致闪烁）
      // WebM 优先（体积更小、画质更好），MP4 作为 fallback（兼容 iOS Safari）
      const sourceWebm = document.createElement('source');
      sourceWebm.src = 'https://xuyang-media-1416484933.cos.ap-guangzhou.myqcloud.com/portfolio%20videos/home/hero-bg.webm';
      sourceWebm.type = 'video/webm';

      const sourceMp4 = document.createElement('source');
      sourceMp4.src = 'https://xuyang-media-1416484933.cos.ap-guangzhou.myqcloud.com/portfolio%20videos/home/hero-bg.mp4';
      sourceMp4.type = 'video/mp4';

      // 3. 视频可播放时淡入
      video.addEventListener('canplay', () => {
        heroBg.classList.add('video-ready');
      }, { once: true });

      // 如果视频已缓存（readyState >= 3 即 canplay）
      if (video.readyState >= 3) {
        heroBg.classList.add('video-ready');
      }

      // 添加源并触发加载（浏览器会自动选择支持的格式）
      video.appendChild(sourceWebm);
      video.appendChild(sourceMp4);
      video.load();

      // 4. 页面不可见时暂停视频（省电/省资源）
      document.addEventListener('visibilitychange', () => {
        if (document.hidden) {
          video.pause();
        } else {
          video.play().catch(() => {});
        }
      });

      // 5. 滚动超出 Hero 区域时暂停视频
      const hero = document.querySelector('.hero');
      if (hero) {
        const observer = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              video.play().catch(() => {});
            } else {
              video.pause();
            }
          });
        }, { threshold: 0.1 });
        observer.observe(hero);
      }
    })();
  </script>



  <!-- Hero区域 - 视频背景 -->
  <section class="hero">
    <div class="hero-bg">
      <!-- 视频背景：autoplay + muted + loop + playsinline 确保自动静音播放 -->
      <video
        class="hero-video"
        autoplay
        muted
        loop
        playsinline
        webkit-playsinline
        preload="auto"
      >
        <!--
          推荐视频规格：
          ▸ 格式：MP4 (H.264 编码) — 兼容性最佳
          ▸ 备选：WebM (VP9 编码) — 更小体积、更好画质
          ▸ 分辨率：1920×1080 (16:9) — 适配主流屏幕
          ▸ 帧率：24-30fps — 背景视频无需高帧率
          ▸ 码率：2-4 Mbps — 平衡画质与文件大小
          ▸ 时长：10-30秒 — 循环播放，短即可
          ▸ 文件大小：≤ 8MB — 确保快速加载
          ▸ 音频：移除音轨 — 减少体积

          视频源通过 JS 动态注入，避免浏览器提前加载导致闪烁
        -->
      </video>
    </div>
    <div class="hero-gradient"></div>
    <div class="hero-content">
      <h1 class="hero-title" data-i18n="index.heroTitle">GAME UX DESIGN</h1>
      <p class="hero-subtitle" data-i18n="index.heroSubtitle">Crafting Immersive FPS Experiences & Cross-Platform Interaction Innovation</p>
      <p class="hero-desc" data-i18n="index.heroDesc">A decade of dedication, from CrossFire to Counter-Strike: Future. I seamlessly blend core gameplay, narrative emotion and commercial value, using design to connect player instinct with the game world.</p>
    </div>

    <!-- Stats - Bottom Left -->
    <div class="hero-stats">
      <div class="stat-item">
        <span class="stat-number">6</span>
        <span class="stat-label" data-i18n-html="index.statProjects">Project<br/>Experience</span>
      </div>
      <div class="stat-divider"></div>
      <div class="stat-item">
        <span class="stat-number">6</span>
        <span class="stat-label" data-i18n-html="index.statCategories">Design<br/>Dimensions</span>
      </div>
      <div class="stat-divider"></div>
      <div class="stat-item">
        <span class="stat-number">500+</span>
        <span class="stat-label" data-i18n-html="index.statLatest">Design Solutions<br/>Delivered</span>
      </div>
    </div>

    <div class="hero-scroll-indicator">
      <div class="scroll-line"></div>
    </div>
  </section>

  <!-- Design Abilities Section -->
  <section class="abilities-section">
    <div class="section-header reveal">
      <span class="section-tag" data-i18n="index.abilitiesTag">What I Do</span>
      <h2 class="section-title" data-i18n="index.abilitiesTitle">DESIGN ABILITIES</h2>
      <p class="section-subtitle" data-i18n="index.abilitiesSubtitle">With years of experience in game UX/UI design, I bring a comprehensive skill set spanning thematic storytelling, cross-platform adaptation, core gameplay interaction, monetization strategy, and systematic design thinking — delivering cohesive and player-centered experiences at every touchpoint.</p>
    </div>
    <div class="abilities-grid reveal-stagger">
      <div class="ability-card" data-tilt>
        <div class="ability-icon">
          <img src="/home/abilities/thematic-design.webp" alt="Thematic Experience Design" loading="lazy" />
        </div>
        <h3 class="ability-name" data-i18n="index.ability1Name">THEMATIC EXPERIENCE DESIGN</h3>
        <p class="ability-desc" data-i18n="index.ability1Desc">Making the interface an extension of the game world through stylized layouts and lore-aligned interactions.</p>
      </div>
      <div class="ability-card" data-tilt>
        <div class="ability-icon">
          <img src="/home/abilities/cross-platform.webp" alt="Cross-Platform UX Design" loading="lazy" />
        </div>
        <h3 class="ability-name" data-i18n="index.ability2Name">CROSS-PLATFORM UX DESIGN</h3>
        <p class="ability-desc" data-i18n="index.ability2Desc">Tailoring optimal interaction solutions across mobile, PC, console and XR devices.</p>
      </div>
      <div class="ability-card" data-tilt>
        <div class="ability-icon">
          <img src="/home/abilities/core-gameplay.webp" alt="Gameplay Interaction Design" loading="lazy" />
        </div>
        <h3 class="ability-name" data-i18n="index.ability3Name">GAMEPLAY INTERACTION DESIGN</h3>
        <p class="ability-desc" data-i18n="index.ability3Desc">Precise, fluid and distraction-free interaction design for combat and core mechanics.</p>
      </div>
      <div class="ability-card" data-tilt>
        <div class="ability-icon">
          <img src="/home/abilities/monetization.webp" alt="Monetization UX Design" loading="lazy" />
        </div>
        <h3 class="ability-name" data-i18n="index.ability4Name">MONETIZATION UX DESIGN</h3>
        <p class="ability-desc" data-i18n="index.ability4Desc">Seamlessly integrating monetization systems into the player journey with elegant design.</p>
      </div>
      <div class="ability-card" data-tilt>
        <div class="ability-icon">
          <img src="/home/abilities/system-design.webp" alt="System Design" loading="lazy" />
        </div>
        <h3 class="ability-name" data-i18n="index.ability5Name">SYSTEM DESIGN</h3>
        <p class="ability-desc" data-i18n="index.ability5Desc">Building scalable component libraries and design tokens for visual consistency across products.</p>
      </div>
      <div class="ability-card" data-tilt>
        <div class="ability-icon">
          <img src="/home/abilities/creative-solution.webp" alt="Creative Experience Solution" loading="lazy" />
        </div>
        <h3 class="ability-name" data-i18n="index.ability6Name">CREATIVE EXPERIENCE SOLUTION</h3>
        <p class="ability-desc" data-i18n="index.ability6Desc">Crafting innovative and memorable player experiences through creative design thinking and storytelling.</p>
      </div>
    </div>
  </section>

  <!-- Featured Projects Section -->
  <section class="projects-section">
    <div class="section-header reveal">
      <span class="section-tag" data-i18n="index.projectsTag">Featured Work</span>
      <h2 class="section-title" data-i18n="index.projectsTitle">DESIGN CATEGORIES</h2>
    </div>
    
    <div class="projects-list">
      {(() => {
        const categoryMap = new Map();
        works.works.forEach((work: any) => {
          if (!categoryMap.has(work.category)) {
            categoryMap.set(work.category, work);
          }
        });
        return Array.from(categoryMap.entries());
      })().map(([category, work]: [string, any], idx: number) => (
        <a
          href={`/works?category=${encodeURIComponent(category)}`}
          class={`category-row ${idx % 2 === 0 ? 'row-left' : 'row-right'}`}
          data-index={String(idx + 1).padStart(2, '0')}
        >
          <div class="category-row-image">
            <img src={work.cardCover || work.cover} alt={category} loading="lazy">
            <div class="category-row-image-overlay"></div>
          </div>
          <div class="category-row-text">
            <span class="category-row-index">{String(idx + 1).padStart(2, '0')} / 06</span>
            <h3 class="category-row-title" data-i18n-category={category}>{category}</h3>
            <span class="category-row-divider"></span>
            <p class="category-row-tagline" data-i18n={categoryTaglines[category]?.i18nKey || ''}>{categoryTaglines[category]?.text || ''}</p>
            <p class="category-row-desc" data-i18n={categoryDescriptions[category]?.i18nKey || ''}>{categoryDescriptions[category]?.text || ''}</p>
            <div class="category-row-meta">
              <span class="category-row-count" data-i18n-projects-count={works.works.filter((w: any) => w.category === category).length}>{works.works.filter((w: any) => w.category === category).length} Projects</span>
              <span class="category-row-dot">·</span>
              <span class="category-row-year">{work.year}</span>
            </div>
            <span class="category-row-cta">
              <span data-i18n="index.viewProjects">View Projects</span>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
            </span>
          </div>
        </a>
      ))}
    </div>

    <div class="section-cta">
      <a href="/works" class="btn-view-all">
        <span data-i18n="index.viewAll">View All Projects</span>
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
      </a>
    </div>
  </section>

  <!-- CTA区域 -->
  <section class="cta-section">
    <div class="cta-bg"></div>
    <div class="cta-content reveal">
      <span class="cta-tag" data-i18n="index.ctaTag">Get in Touch</span>
      <h2 data-i18n-html="index.ctaTitle">Let's Create Something<br/>Amazing Together</h2>
      <p data-i18n="index.ctaDesc">I'd love to hear about it and discuss how we can bring your vision to life.</p>
    </div>
  </section>

  <!-- 页脚 -->
  <footer class="site-footer">
    <div class="footer-inner">
      <div class="footer-top">
        <div class="footer-brand">
          <div class="footer-logo">
            <img src="/common/logo.webp" alt="XUYANG UX DESIGN" class="footer-logo-img" />
          </div>
          <p data-i18n="footer.siteDesc">{config.site.description}</p>
        </div>
        <div class="footer-nav">
          <div class="footer-col">
            <h4 data-i18n="footer.navigation">Navigation</h4>
            <a href="/" data-i18n="footer.home">Home</a>
            <a href="/works" data-i18n="footer.work">Work</a>
            <a href="/about" data-i18n="footer.about">About</a>
          </div>
          <div class="footer-col">
            <h4 data-i18n="footer.contact">Contact</h4>
            <a href="/about#contact" data-i18n="footer.getInTouch">Get in Touch</a>
            <a href="mailto:{config.contact.email || '#'}" data-i18n="footer.emailMe">Email Me</a>
          </div>
        </div>
      </div>
      <div class="footer-bottom">
        <p>&copy; 2026 {config.site.name}. <span data-i18n="footer.rights">All rights reserved.</span></p>
        <p class="footer-credit" data-i18n="footer.credit">Designed with passion</p>
      </div>
    </div>
  </footer>
  <script is:inline src="/js/i18n.js"></script>
  <link rel="stylesheet" href="/css/sound.css">
  <script is:inline src="/js/sound.js"></script>

  <!-- 滚动渐入动画观察器 -->
  <script>
    (function() {
      const revealElements = document.querySelectorAll('.reveal, .reveal-stagger');
      if (!revealElements.length) return;

      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('visible');
            observer.unobserve(entry.target);
          }
        });
      }, { threshold: 0.15, rootMargin: '0px 0px -40px 0px' });

      revealElements.forEach(el => observer.observe(el));

      // Hero 入场动画
      const heroContent = document.querySelector('.hero-content');
      const heroStats = document.querySelector('.hero-stats');
      if (heroContent) {
        heroContent.style.opacity = '0';
        heroContent.style.transform = 'translateY(30px)';
        setTimeout(() => {
          heroContent.style.transition = 'opacity 1s cubic-bezier(0.16, 1, 0.3, 1), transform 1s cubic-bezier(0.16, 1, 0.3, 1)';
          heroContent.style.opacity = '1';
          heroContent.style.transform = 'translateY(0)';
        }, 300);
      }
      if (heroStats) {
        heroStats.style.opacity = '0';
        heroStats.style.transform = 'translateY(20px)';
        setTimeout(() => {
          heroStats.style.transition = 'opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1), transform 0.8s cubic-bezier(0.16, 1, 0.3, 1)';
          heroStats.style.opacity = '1';
          heroStats.style.transform = 'translateY(0)';
        }, 800);
      }

      // Category rows staggered reveal
      const cards = document.querySelectorAll('.category-row');
      const cardObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('card-visible');
            cardObserver.unobserve(entry.target);
          }
        });
      }, { threshold: 0.1, rootMargin: '0px 0px -30px 0px' });
      cards.forEach((card, i) => {
        card.style.transitionDelay = `${i * 0.12}s`;
        cardObserver.observe(card);
      });
    })();
  </script>

  <!-- 3D 透视倾斜效果 -->
  <script>
    (function() {
      const tiltCards = document.querySelectorAll('[data-tilt]');
      if (!tiltCards.length) return;

      // 是否为触屏设备（触屏不启用tilt）
      const isTouchDevice = 'ontouchstart' in window || navigator.maxTouchPoints > 0;
      if (isTouchDevice) return;

      tiltCards.forEach(card => {
        const maxTilt = parseFloat(card.getAttribute('data-tilt-max') || '12');
        const perspective = 800;
        let rafId = null;
        let currentRotateX = 0;
        let currentRotateY = 0;
        let currentTranslateY = 0;
        let currentScale = 1;
        let targetRotateX = 0;
        let targetRotateY = 0;
        let targetTranslateY = 0;
        let targetScale = 1;
        let isHovering = false;

        // 判断是能力卡片还是项目卡片
        const isAbilityCard = card.classList.contains('ability-card');
        const isProjectCard = card.classList.contains('project-card');
        // ability-card 不需要悬停动画效果
        if (isAbilityCard) return;
        const hoverScale = 1;
        const hoverLift = -8;

        function lerp(start, end, factor) {
          return start + (end - start) * factor;
        }

        function animate() {
          if (!isHovering && 
              Math.abs(currentRotateX) < 0.01 && 
              Math.abs(currentRotateY) < 0.01 &&
              Math.abs(currentTranslateY) < 0.01 &&
              Math.abs(currentScale - 1) < 0.001) {
            card.style.transform = '';
            rafId = null;
            return;
          }

          const speed = isHovering ? 0.12 : 0.08;
          currentRotateX = lerp(currentRotateX, targetRotateX, speed);
          currentRotateY = lerp(currentRotateY, targetRotateY, speed);
          currentTranslateY = lerp(currentTranslateY, targetTranslateY, speed);
          currentScale = lerp(currentScale, targetScale, speed);

          // 项目卡片需要保持 card-visible 的 translateY(0) 和 opacity
          if (isProjectCard && card.classList.contains('card-visible')) {
            card.style.transform = `perspective(${perspective}px) rotateX(${currentRotateX}deg) rotateY(${currentRotateY}deg) translateY(${currentTranslateY}px) scale(${currentScale})`;
            card.style.opacity = '1';
          } else {
            card.style.transform = `perspective(${perspective}px) rotateX(${currentRotateX}deg) rotateY(${currentRotateY}deg) translateY(${currentTranslateY}px) scale(${currentScale})`;
          }

          rafId = requestAnimationFrame(animate);
        }

        card.addEventListener('mousemove', function(e) {
          const rect = card.getBoundingClientRect();
          const x = e.clientX - rect.left;
          const y = e.clientY - rect.top;
          const centerX = rect.width / 2;
          const centerY = rect.height / 2;

          // 计算倾斜角度
          targetRotateX = -((y - centerY) / centerY) * maxTilt;
          targetRotateY = ((x - centerX) / centerX) * maxTilt;
          targetTranslateY = hoverLift;
          targetScale = hoverScale;


          if (!rafId) {
            rafId = requestAnimationFrame(animate);
          }
        });

        card.addEventListener('mouseenter', function() {
          isHovering = true;
          card.style.transition = 'box-shadow 0.05s ease, border-color 0.05s ease, background 0.05s ease';
          if (!rafId) {
            rafId = requestAnimationFrame(animate);
          }
        });

        card.addEventListener('mouseleave', function() {
          isHovering = false;
          targetRotateX = 0;
          targetRotateY = 0;
          targetTranslateY = 0;
          targetScale = 1;
          card.style.transition = 'transform 0.1s ease, box-shadow 0.05s ease, border-color 0.05s ease, background 0.05s ease';
          if (!rafId) {
            rafId = requestAnimationFrame(animate);
          }
        });
      });
    })();
  </script>
</body>
</html>

<style>
  /* ===== 导航栏 ===== */
  .site-header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 1000;
    padding: 1.8rem var(--container-padding);
    background: transparent;
    transition: all 0.4s var(--ease-out);
  }

  .site-header.scrolled {
    background: rgba(8, 8, 10, 0.92);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    padding-top: 0.9rem;
    padding-bottom: 0.9rem;
    border-bottom: 1px solid var(--border);
  }

  .header-inner {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: relative;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    text-decoration: none;
    color: var(--text-primary);
    flex-shrink: 0;
  }

  .logo-img {
    height: 48px;
    width: auto;
    display: block;
    object-fit: contain;
  }

  .nav-right {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }

  .main-nav {
    display: flex;
    gap: 2.5rem;
    align-items: center;
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
  }

  .nav-link {
    color: var(--text-secondary);
    text-decoration: none;
    font-size: 0.8rem;
    font-weight: 500;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    transition: color 0.3s ease;
    position: relative;
    padding: 0.25rem 0;
  }

  .nav-link.active,
  .nav-link:hover {
    color: var(--text-primary);
  }

  .nav-link::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 0;
    height: 1.5px;
    background: var(--accent);
    transition: width 0.3s var(--ease-out);
  }

  .nav-link.active::after,
  .nav-link:hover::after {
    width: 100%;
  }

  /* ===== 语言切换按钮 ===== */
  .lang-toggle {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    background: transparent;
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 6px;
    padding: 0.4rem 0.7rem;
    cursor: pointer;
    transition: all 0.3s ease;
    color: var(--text-secondary);
    font-family: inherit;
  }

  .lang-toggle:hover {
    border-color: var(--accent);
    color: var(--accent);
    background: rgba(45, 212, 191, 0.05);
  }

  .lang-icon {
    width: 18px;
    height: 18px;
    object-fit: contain;
    opacity: 0.85;
    transition: opacity 0.3s;
  }

  .lang-toggle:hover .lang-icon {
    opacity: 1;
  }

  .lang-label {
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .nav-cta {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: #08080a;
    text-decoration: none;
    font-size: 0.78rem;
    font-family: 'Inter', sans-serif;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    padding: 0.6rem 1.5rem;
    background: var(--accent);
    border: 1px solid var(--accent);
    border-radius: 0;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .nav-cta::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, transparent 40%, rgba(255,255,255,0.25) 50%, transparent 60%);
    background-size: 250% 250%;
    animation: btnShine 4s ease-in-out infinite;
  }

  @keyframes btnShine {
    0%, 100% { background-position: 200% 0; }
    50% { background-position: -50% 0; }
  }

  .nav-cta:hover {
    background: #3dd4c6;
    border-color: #3dd4c6;
    transform: translateY(-1px);
    box-shadow: 0 4px 20px rgba(78, 234, 219, 0.3);
  }

  /* ===== Hero ===== */
  .hero {
    position: relative;
    width: 100%;
    height: 800px;
    display: flex;
    align-items: flex-start;
    justify-content: flex-start;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background: #08080a;
  }

  /* 视频 — 铺满容器，默认隐藏，加载完成后淡入 */
  .hero-video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(0.6) saturate(1.1);
    z-index: 1;
    opacity: 0;
    transition: opacity 0.8s ease;
  }

  /* 视频加载完成后淡入 */
  .hero-bg.video-ready .hero-video {
    opacity: 1;
  }

  /* prefers-reduced-motion：尊重用户系统设置 */
  @media (prefers-reduced-motion: reduce) {
    .hero-video {
      display: none !important;
    }
  }

  .hero-gradient {
    position: absolute;
    inset: 0;
    background: 
      linear-gradient(90deg, rgba(8,8,10,0.3) 0%, rgba(8,8,10,0.15) 35%, rgba(8,8,10,0.05) 65%, transparent 100%),
      linear-gradient(180deg, rgba(8,8,10,0.2) 0%, rgba(8,8,10,0.02) 40%, rgba(8,8,10,0.25) 80%, var(--bg-primary) 100%),
      radial-gradient(ellipse at 20% 30%, rgba(78,234,219,0.06) 0%, transparent 60%);
  }

  .hero-content {
    position: relative;
    z-index: 2;
    text-align: left;
    max-width: 700px;
    padding: 0 var(--container-padding);
    margin-top: clamp(120px, 18vh, 180px);
    margin-left: clamp(2rem, 8vw, 10rem);
  }

  .hero-title {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: clamp(2.4rem, 5.5vw, 4rem);
    font-weight: 700;
    letter-spacing: 0.04em;
    line-height: 1.1;
    word-break: keep-all;
    overflow-wrap: break-word;
    margin-bottom: 0.8rem;
    color: rgba(255, 255, 255, 0.92);
  }

  .hero-subtitle {
    font-size: clamp(0.9rem, 1.5vw, 1.15rem);
    color: var(--accent-secondary);
    font-weight: 500;
    letter-spacing: 0.04em;
    margin-bottom: 1.2rem;
    line-height: 1.6;
  }

  .hero-desc {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.55);
    line-height: 1.8;
    max-width: 440px;
    margin: 0 0 2.2rem 0;
  }

  /* Hero CTA 按钮 - Kevuru Games 风格 */
  .hero-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.85rem;
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #08080a;
    background: var(--accent);
    border: none;
    border-radius: 0;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .hero-cta:hover {
    background: #3dd4c6;
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(78, 234, 219, 0.25);
  }

  .hero-cta svg {
    transition: transform 0.3s ease;
    stroke: #08080a;
  }

  .hero-cta:hover svg {
    transform: translate(2px, -2px);
  }

  /* Hero 数据统计 - 左下角 */
  .hero-stats {
    position: absolute;
    bottom: 3.5rem;
    left: clamp(2rem, 8vw, 10rem);
    z-index: 3;
    display: flex;
    align-items: center;
    gap: clamp(1rem, 2.5vw, 3rem);
    padding: clamp(0.8rem, 1.3vw, 1.5rem) clamp(1.2rem, 2.5vw, 3rem);
    background: rgba(255, 255, 255, 0.04);
    backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 16px;
    white-space: nowrap;
  }

  .stat-item {
    display: flex;
    align-items: center;
    gap: clamp(0.4rem, 0.8vw, 1rem);
    text-align: left;
  }

  .stat-number {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: clamp(1.4rem, 2vw, 2.4rem);
    font-weight: 700;
    color: var(--accent);
    letter-spacing: -0.01em;
    line-height: 1;
  }

  .stat-label {
    font-size: clamp(0.5rem, 0.6vw, 0.72rem);
    font-family: 'Inter', sans-serif;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    line-height: 1.4;
  }

  .stat-divider {
    width: 1px;
    height: clamp(20px, 3vw, 36px);
    background: var(--border);
  }

  .hero-scroll-indicator {
    position: absolute;
    bottom: 2rem;
    right: clamp(2rem, 5vw, 5rem);
    z-index: 3;
  }

  .scroll-line {
    width: 1px;
    height: 0;
    background: rgba(255, 255, 255, 0.3);
    animation: scrollLine 2s var(--ease-in-out) infinite;
  }

  @keyframes scrollLine {
    0% { height: 0; opacity: 0; }
    50% { height: 40px; opacity: 1; }
    100% { height: 40px; opacity: 0; }
  }

  /* ===== 设计能力分类区域 ===== */
  .abilities-section {
    padding: 5rem var(--container-padding) 2rem;
    background: var(--bg-primary);
    position: relative;
  }

  /* 微妙的网格纹理背景 */
  .abilities-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(rgba(78, 234, 219, 0.02) 1px, transparent 1px),
      linear-gradient(90deg, rgba(78, 234, 219, 0.02) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse at 50% 30%, black 0%, transparent 70%);
    -webkit-mask-image: radial-gradient(ellipse at 50% 30%, black 0%, transparent 70%);
    pointer-events: none;
  }

  .abilities-grid {
    width: 100%;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    gap: 1rem;
    justify-items: stretch;
  }

  .ability-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 2.4rem 1.2rem 2.2rem;
    border-radius: 0;
    border: 1px solid rgba(255, 255, 255, 0.2);
    background: rgba(8, 8, 10, 0.6);
    transition: none;
    position: relative;
    overflow: hidden;
    width: 100%;
    transform-style: flat;
    will-change: auto;
  }


  .ability-card:hover {
    border-color: rgba(255, 255, 255, 0.3);
    background: #ffffff;
    box-shadow: none;
  }

  .ability-icon {
    width: 88px;
    height: 88px;
    margin-bottom: 1.4rem;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    z-index: 1;
  }

  .ability-icon img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    filter: brightness(0) invert(1) opacity(0.85);
    transition: none;
  }

  .ability-card:hover .ability-icon img {
    filter: brightness(0) invert(0) opacity(0.85);
    transform: none;
  }

  .ability-name {
    font-size: 1.05rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #ffffff;
    margin-bottom: 0.75rem;
    position: relative;
    z-index: 1;
    word-break: break-word;
    transition: none;
  }

  .ability-card:hover .ability-name {
    color: #1a1a2e;
  }

  .ability-desc {
    font-size: 0.82rem;
    color: var(--text-secondary);
    line-height: 1.6;
    position: relative;
    z-index: 1;
    margin: 0;
    transition: none;
  }

  .ability-card:hover .ability-desc {
    color: #3a3a5c;
  }

  /* ===== 作品展示区 - Kevuru Games 卡片风格 ===== */
  .projects-section {
    padding: 3rem var(--container-padding) var(--section-padding);
    background: var(--bg-primary);
  }

  .section-header {
    text-align: center;
    margin-bottom: 1.75rem;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  .section-tag {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 0.72rem;
    font-family: 'Inter', sans-serif;
    font-weight: 600;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 1.2rem;
  }

  .section-tag::before,
  .section-tag::after {
    content: '';
    width: 24px;
    height: 1px;
    background: var(--accent);
    opacity: 0.4;
  }

  .section-title {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: clamp(1.6rem, 3vw, 2.2rem);
    font-weight: 700;
    letter-spacing: 0.06em;
    line-height: 1.15;
    color: var(--text-primary);
  }

  .section-subtitle {
    font-size: 0.95rem;
    color: var(--text-secondary);
    line-height: 1.75;
    margin-top: 1.5rem;
    max-width: 680px;
    margin-left: auto;
    margin-right: auto;
  }

  /* ===== 类目行（左图右文 / 右图左文 交替） ===== */
  .projects-list {
    width: 100%;
    max-width: 1280px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 4rem;
  }

  .category-row {
    position: relative;
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: stretch;
    gap: 0;
    text-decoration: none;
    color: inherit;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.06);
    background: linear-gradient(180deg, rgba(20, 20, 28, 0.4) 0%, rgba(12, 12, 18, 0.6) 100%);
    min-height: 420px;
    /* 进场动画 */
    opacity: 0;
    transform: translateY(40px);
    transition: opacity 0.6s ease, transform 0.6s ease, border-color 0.3s ease, box-shadow 0.3s ease;
    will-change: transform;
  }

  .category-row.card-visible {
    opacity: 1;
    transform: translateY(0);
  }

  .category-row:hover,
  .category-row:active,
  .category-row:focus,
  .category-row:focus-visible {
    background: #ffffff;
    border-color: transparent;
    outline: none;
  }

  .category-row:hover .category-row-title,
  .category-row:active .category-row-title,
  .category-row:focus .category-row-title,
  .category-row:focus-visible .category-row-title {
    color: #0f0f15;
  }

  .category-row:hover .category-row-tagline,
  .category-row:active .category-row-tagline,
  .category-row:focus .category-row-tagline,
  .category-row:focus-visible .category-row-tagline {
    color: rgba(15, 15, 21, 0.85);
  }

  .category-row:hover .category-row-desc,
  .category-row:active .category-row-desc,
  .category-row:focus .category-row-desc,
  .category-row:focus-visible .category-row-desc {
    color: #1a1a22;
  }

  .category-row:hover .category-row-meta,
  .category-row:active .category-row-meta,
  .category-row:focus .category-row-meta,
  .category-row:focus-visible .category-row-meta {
    color: rgba(15, 15, 21, 0.7);
  }

  .category-row:hover .category-row-dot,
  .category-row:active .category-row-dot,
  .category-row:focus .category-row-dot,
  .category-row:focus-visible .category-row-dot {
    color: rgba(15, 15, 21, 0.5);
  }

  /* 偶数行：右图左文（image 移到右侧） */
  .category-row.row-right .category-row-image {
    order: 2;
  }
  .category-row.row-right .category-row-text {
    order: 1;
  }

  /* === 图片区 === */
  .category-row-image {
    position: relative;
    overflow: hidden;
    min-height: 420px;
  }

  .category-row-image img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    transition: transform 0.6s ease, filter 0.6s ease;
    will-change: transform;
  }

  .category-row:hover .category-row-image img,
  .category-row:active .category-row-image img,
  .category-row:focus .category-row-image img,
  .category-row:focus-visible .category-row-image img {
    transform: scale(1.06);
    filter: brightness(1.05);
  }

  .category-row-image-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(8, 8, 12, 0.15) 0%, transparent 60%);
    pointer-events: none;
  }

  /* === 文字区 === */
  .category-row-text {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 2.6rem 3rem;
    gap: 0.9rem;
  }

  .category-row-index {
    font-family: 'Inter', sans-serif;
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.18em;
    color: var(--accent);
    text-transform: uppercase;
  }

  .category-row-title {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: clamp(1.5rem, 2.4vw, 2.2rem);
    font-weight: 700;
    line-height: 1.18;
    letter-spacing: 0.01em;
    text-transform: uppercase;
    color: #ffffff;
    margin: 0;
    transition: color 0.3s ease;
  }

  /* 悬停时整张卡片切换为白底深字（title/tagline/desc/cta 见上方 :hover 规则） */

  :global(html[lang="en"]) .category-row-title {
    font-size: clamp(1.25rem, 2vw, 1.85rem);
  }

  .category-row-divider {
    display: block;
    width: 48px;
    height: 2px;
    background: var(--accent);
    margin: 0.2rem 0 0.4rem;
    transition: width 0.4s ease;
  }

  .category-row:hover .category-row-divider,
  .category-row:active .category-row-divider,
  .category-row:focus .category-row-divider,
  .category-row:focus-visible .category-row-divider {
    width: 80px;
  }

  .category-row-tagline {
    font-size: 0.82rem;
    color: rgba(255, 255, 255, 0.55);
    font-weight: 400;
    letter-spacing: 0.06em;
    line-height: 1.45;
    margin: 0;
  }

  .category-row-desc {
    font-size: 0.92rem;
    color: rgba(255, 255, 255, 0.72);
    line-height: 1.7;
    margin: 0;
    max-width: 520px;
  }

  .category-row-meta {
    display: flex;
    align-items: center;
    gap: 0.7rem;
    margin-top: 0.4rem;
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.45);
  }

  .category-row-dot {
    color: rgba(255, 255, 255, 0.3);
  }

  .category-row-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    margin-top: 1rem;
    font-family: 'Inter', sans-serif;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--accent);
    transition: gap 0.3s ease, color 0.3s ease;
  }

  .category-row:hover .category-row-cta,
  .category-row:active .category-row-cta,
  .category-row:focus .category-row-cta,
  .category-row:focus-visible .category-row-cta {
    gap: 0.95rem;
    color: #0f0f15;
  }

  .category-row-cta svg {
    transition: transform 0.3s ease;
    stroke: currentColor;
  }

  .category-row:hover .category-row-cta svg,
  .category-row:active .category-row-cta svg,
  .category-row:focus .category-row-cta svg,
  .category-row:focus-visible .category-row-cta svg {
    transform: translateX(4px);
  }

  .section-cta {
    text-align: center;
    margin-top: 4rem;
  }

  .btn-view-all {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    width: 240px;
    padding: 0.85rem 0;
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    color: #08080a;
    background: var(--accent);
    border: none;
    border-radius: 0;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .btn-view-all:hover {
    background: #3dd4c6;
    transform: translateY(-2px);
  }

  /* ===== 游戏 Logo 展示栏 ===== */
  .logo-marquee-section {
    padding: 2.5rem 0;
    background: var(--bg-primary);
    position: relative;
  }

  .logo-marquee-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 10%;
    right: 10%;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--border-hover), transparent);
  }

  .logo-marquee-section::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 10%;
    right: 10%;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--border-hover), transparent);
  }

  .logo-marquee {
    position: relative;
    width: 100%;
    padding: 12px 0;
    display: flex;
    justify-content: center;
  }

  .logo-track {
    display: flex;
    gap: 4rem;
    justify-content: center;
    align-items: center;
  }

  .logo-item {
    position: relative;
    flex-shrink: 0;
    width: 60px;
    height: 60px;
    border-radius: 0;
    overflow: hidden;
    border: 1px solid transparent;
    transition: transform 0.35s var(--ease-out), box-shadow 0.35s var(--ease-out), border-color 0.35s ease;
    cursor: pointer;
  }

  .logo-item:hover {
    transform: scale(1.1);
    border-color: transparent;
    box-shadow: none;
  }

  .logo-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    border-radius: 0;
    transition: filter 0.35s ease;
  }

  .logo-item:hover img {
    filter: brightness(1.2) saturate(1.15);
  }

  .logo-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border-radius: 0;
    background: linear-gradient(to top, rgba(0, 0, 0, 0.6) 0%, rgba(0, 0, 0, 0.15) 50%, transparent 100%);
    pointer-events: none;
    transition: opacity 0.35s ease;
  }

  .logo-item:hover .logo-overlay {
    opacity: 0;
  }

  /* ===== CTA 区域 ===== */
  .cta-section {
    position: relative;
    padding: var(--section-padding) var(--container-padding);
    text-align: center;
    overflow: hidden;
  }

  .cta-bg {
    position: absolute;
    inset: 0;
    background: var(--bg-secondary);
  }

  .cta-content {
    position: relative;
    z-index: 2;
    max-width: 600px;
    margin: 0 auto;
  }

  .cta-tag {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 0.72rem;
    font-family: 'Inter', sans-serif;
    font-weight: 600;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 1.5rem;
  }

  .cta-tag::before,
  .cta-tag::after {
    content: '';
    width: 24px;
    height: 1px;
    background: var(--accent);
    opacity: 0.4;
  }

  .cta-content h2 {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: clamp(2rem, 4vw, 2.8rem);
    font-weight: 700;
    margin-bottom: 1rem;
    line-height: 1.2;
    color: var(--text-primary);
  }

  .cta-content p {
    font-size: 1rem;
    color: var(--text-secondary);
    margin-bottom: 2.5rem;
    line-height: 1.7;
  }

  .cta-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    align-items: center;
  }

  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.9rem 2.2rem;
    background: var(--accent);
    color: #08080a;
    text-decoration: none;
    border-radius: 0;
    font-weight: 700;
    font-size: 0.875rem;
    letter-spacing: 0.06em;
    transition: all 0.3s ease;
  }

  .btn-primary:hover {
    background: #3dd4c6;
    transform: translateY(-2px);
  }

  .btn-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 240px;
    padding: 0.9rem 0;
    color: var(--text-primary);
    text-decoration: none;
    background: var(--bg-tertiary);
    border: none;
    border-radius: 0;
    font-weight: 700;
    font-size: 0.875rem;
    letter-spacing: 0.06em;
    transition: all 0.3s ease;
  }

  .btn-secondary:hover {
    background: #2a2a30;
  }

  /* ===== 页脚 ===== */
  .site-footer {
    padding: 4rem var(--container-padding) 2rem;
    background: var(--bg-primary);
    border-top: none;
    position: relative;
  }

  .footer-inner {
    width: 100%;
  }

  .footer-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    padding-bottom: 3rem;
    border-bottom: 1px solid var(--border);
    margin-bottom: 2rem;
  }

  .footer-logo {
    display: flex;
    align-items: center;
    margin-bottom: 0.75rem;
  }

  .footer-logo-img {
    height: 48px;
    width: auto;
    object-fit: contain;
  }

  .footer-brand p {
    color: var(--text-tertiary);
    font-size: 0.9rem;
    max-width: 280px;
    line-height: 1.6;
  }

  .footer-nav {
    display: flex;
    gap: 5rem;
  }

  .footer-col h4 {
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--text-secondary);
    margin-bottom: 1rem;
  }

  .footer-col a {
    display: block;
    color: var(--text-tertiary);
    text-decoration: none;
    font-size: 0.9rem;
    padding: 0.3rem 0;
    transition: color 0.2s;
  }

  .footer-col a:hover {
    color: var(--text-primary);
  }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .footer-bottom p {
    color: var(--text-tertiary);
    font-size: 0.8rem;
  }

  .footer-credit {
    font-size: 0.75rem;
    color: var(--text-tertiary);
  }

  /* ===== 响应式 ===== */
  @media (max-width: 1024px) {
    .projects-list {
      gap: 3rem;
    }

    .category-row {
      min-height: 360px;
    }

    .category-row-image {
      min-height: 360px;
    }

    .category-row-text {
      padding: 2rem 2.2rem;
      gap: 0.7rem;
    }

    .category-row-title {
      font-size: 1.5rem;
    }

    :global(html[lang="en"]) .category-row-title {
      font-size: 1.25rem;
    }

    .category-row-tagline {
      font-size: 0.78rem;
    }

    .category-row-desc {
      font-size: 0.86rem;
    }

    .abilities-grid {
      grid-template-columns: repeat(3, 1fr);
    }

    .ability-card {
      padding: 2rem 1rem 1.8rem;
    }

    .ability-icon {
      width: 72px;
      height: 72px;
      margin-bottom: 1.1rem;
    }
  }

  /* ===== 汉堡菜单按钮 ===== */
  .hamburger {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 32px;
    height: 32px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 4px;
    z-index: 1001;
  }

  .hamburger-line {
    display: block;
    width: 100%;
    height: 2px;
    background: var(--text-primary);
    border-radius: 2px;
    transition: all 0.3s ease;
    transform-origin: center;
  }

  .hamburger.active .hamburger-line:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px);
  }

  .hamburger.active .hamburger-line:nth-child(2) {
    opacity: 0;
  }

  .hamburger.active .hamburger-line:nth-child(3) {
    transform: rotate(-45deg) translate(5px, -5px);
  }

  @media (max-width: 768px) {
    .abilities-section {
      padding: 3rem 1.5rem 1.5rem;
    }

    .abilities-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 0.7rem;
    }

    .ability-card {
      padding: 1.5rem 0.9rem 1.5rem;
    }

    .ability-icon {
      width: 60px;
      height: 60px;
      margin-bottom: 0.9rem;
    }

    .ability-name {
      font-size: 0.9rem;
      letter-spacing: 0.06em;
      margin-bottom: 0.5rem;
    }

    .ability-desc {
      font-size: 0.75rem;
      line-height: 1.5;
    }

    .site-header {
      padding: 1rem 1.5rem;
    }

    .site-header.scrolled {
      padding-top: 0.7rem;
      padding-bottom: 0.7rem;
    }

    .logo-img {
      height: 40px;
    }

    .nav-right {
      gap: 0.8rem;
    }

    .nav-cta {
      display: none;
    }

    .lang-toggle {
      order: 0;
    }

    .hamburger {
      display: flex;
    }

    .main-nav {
      position: fixed;
      top: 0;
      right: -100%;
      left: auto;
      transform: none;
      width: 70%;
      max-width: 300px;
      height: 100vh;
      background: rgba(40, 42, 54, 0.92);
      backdrop-filter: blur(24px);
      -webkit-backdrop-filter: blur(24px);
      flex-direction: column;
      align-items: flex-start;
      justify-content: flex-start;
      padding: 6rem 2.5rem 2rem;
      gap: 0;
      transition: right 0.35s ease;
      z-index: 999;
      border-left: 1px solid var(--border);
    }

    .main-nav.open {
      right: 0;
    }

    .nav-link {
      font-size: 1.1rem;
      padding: 1rem 0;
      width: 100%;
      border-bottom: 1px solid var(--border);
      letter-spacing: 0.08em;
    }

    .hero-title {
      font-size: 2.2rem;
    }

    .hero {
      height: 600px;
      flex-direction: column;
      justify-content: flex-start;
    }

    .hero-content {
      margin-top: 120px;
      margin-left: 1.5rem;
      max-width: calc(100% - 3rem);
      padding-bottom: 1rem;
    }

    .hero-desc {
      font-size: 0.9rem;
      max-width: 100%;
    }

    .hero-cta {
      font-size: 0.75rem;
      padding: 0.6rem 1.4rem;
    }

    .hero-stats {
      left: 1.5rem;
      bottom: 2rem;
    }

    .hero-scroll-indicator {
      display: none;
    }

    .projects-section {
      padding: 2rem 1.5rem 4rem;
    }

    .section-header {
      margin-bottom: 3rem;
    }

    .projects-list {
      grid-template-columns: 1fr;
      gap: 2.2rem;
    }

    /* 移动端：行内图文堆叠为单列；左右交替规则失效 */
    .category-row,
    .category-row.row-right {
      display: flex;
      flex-direction: column;
      min-height: auto;
    }

    .category-row.row-right .category-row-image,
    .category-row.row-right .category-row-text {
      order: unset;
    }

    .category-row-image {
      width: 100%;
      min-height: 200px;
      aspect-ratio: 16 / 10;
    }

    .category-row-text {
      padding: 1.6rem 1.5rem 1.8rem;
      gap: 0.6rem;
    }

    .category-row-index {
      font-size: 0.65rem;
    }

    .category-row-title {
      font-size: 1.25rem;
    }

    :global(html[lang="en"]) .category-row-title {
      font-size: 1.05rem;
    }

    .category-row-divider {
      width: 36px;
    }

    .category-row-tagline {
      font-size: 0.74rem;
    }

    .category-row-desc {
      font-size: 0.82rem;
      line-height: 1.6;
    }

    .category-row-meta {
      font-size: 0.65rem;
    }

    .category-row-cta {
      font-size: 0.72rem;
      margin-top: 0.6rem;
    }

    .logo-marquee-section {
      padding: 1.5rem 0;
    }

    .logo-track {
      flex-wrap: wrap;
      gap: 1.5rem 2.5rem;
      max-width: 240px;
    }

    .logo-item {
      width: 46px;
      height: 46px;
      border-radius: 0;
    }

    .logo-item img {
      border-radius: 0;
    }

    .logo-overlay {
      border-radius: 0;
    }

    .cta-section {
    }

    .cta-content h2 {
      font-size: 1.8rem;
    }

    .cta-actions {
      flex-direction: column;
    }

    .footer-top {
      flex-direction: column;
      gap: 2.5rem;
    }

    .footer-nav {
      gap: 3rem;
    }

    .footer-bottom {
      flex-direction: column;
      gap: 0.5rem;
      text-align: center;
    }
  }
</style>
